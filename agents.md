# Repository agent instructions

Read and obey the canonical `ORESoftware/.github` lowercase `agents.md` before
editing. Keep generation deterministic and credential-free. Do not add Jenkins
controller access, secret values, or deployment side effects to the renderer.
